# Configure this as a SERVER secret in Sites, with the same value as the store.
# Never use a VITE_ or NEXT_PUBLIC_ prefix. Minimum length: 32 characters.
COUPON_GAME_API_KEY=
# Local development never contacts the real store unless explicitly enabled.
GAME_ALLOW_STORE_REQUESTS=0
